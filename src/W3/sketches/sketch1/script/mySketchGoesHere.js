let posX;
let posY;
let posXAdd = 3;
let posYAdd = -2;



let pos;
let pos 





function setup() {
  setCanvasContainer('canvas', 3, 2, true);
  background(255);
  posX = width / 2;
  posY = height / 2;
}

function draw() {
  background(255);
  vel.add
  pos = createVector(width/2,height /2);
  pos = createVector(3,5);


if(pos.x<0){

vel.x *= -1;
}else if (pos.x > width){
    vel.x *= -1;

}
윗식 줄여쓰자면

if (pos.x<0|| pos.x>width){
webkitURL.x *= -1;

}
if

  ellipse(pos,X, pos,Y, 50);
  console.log(pos);
  console.log(vel);
  posX++ = 1
  posX=posX+1
  posX = posX +1

  posX+=5
  posY+=3;
}

